# Copy to <name>.env (gitignored) and fill in. Commit THIS file, never the real one.
# For long-lived secrets, prefer a Podman secret over env (ai-dev-ground.md §4):
#   printf '%s' "$KEY" | podman secret create <name>_key -
EXAMPLE_SETTING=value
# ANTHROPIC_API_KEY=        # prefer Secret= in the Quadlet instead of putting it here
