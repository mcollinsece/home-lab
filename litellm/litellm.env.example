# litellm.env — non-secret runtime config for the LiteLLM Docker Compose service.
# Secrets (AWS keys, master key) come from .secrets/*.env — not here.
# Copy to litellm.env; this file is committed, litellm.env is gitignored.

# Uncomment for verbose proxy logging during initial setup / debugging:
# LITELLM_LOG=INFO
