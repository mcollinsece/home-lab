http:
  routers:
    openclaw:
      rule: "Host(`openclaw.lab.lan`)"
      entrypoints: [websecure]
      tls: {}
      service: openclaw
  services:
    openclaw:
      loadBalancer:
        servers:
          - url: "http://172.18.0.1:18789"
